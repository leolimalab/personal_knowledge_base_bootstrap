# Resumos

Camadas de compressão dos diários: `YYYY-Www.md` (semana ISO, por `weekly-review`), `YYYY-MM.md` (mês) e `YYYY-Qn.md` (trimestre), os dois últimos por `compress`. Cada camada resume só a camada de baixo e lista o que ficou de fora.
